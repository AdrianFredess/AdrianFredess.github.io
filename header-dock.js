/**
 * Header contact icons — dock magnification disabled.
 * Hover feedback lives in CSS (background / brightness / color only).
 */
(function initHeaderDock() {
  // Intentionally inactive: no scale / spring on icon hover.
})();
